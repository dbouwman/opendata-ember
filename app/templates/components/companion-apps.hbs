{{#if apps}}
  <span><strong>Open this dataset in:</strong></span> 
  {{#each apps as |app|}}
  <a class="btn btn-link" href="{{app.url}}"> Crime Explorer <span class="glyphicon glyphicon-new-window"></span> </a>
  {{/each}}
{{/if}}
